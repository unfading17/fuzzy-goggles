# Streamlit Secrets (use this as template for your secrets)
# Create a file named secrets.toml in .streamlit/ and add:

DATABASE_URL = "sqlite:///permitai.db"
SECRET_KEY = "your-super-secret-key-here"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 1440
